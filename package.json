{
  "name": "rmdigitalnet",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bundle exec jekyll build",
    "build:cloudflare": "bundle exec jekyll build --config _config.yml,_config_cloudflare.yml",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.113.0"
  }
}
